# データについて

本リポジトリには，公開用としてサンプルの時間割データ（`timetable.csv`）を同梱する．  
実データ（学科配布データ）を用いて動作させる場合は，入手元のデータをCSVに変換し，同一パス（`src/assets/data/timetable.csv`）に配置する．

- 実データは公開用リポジトリには含めない．
- 変換手順や入手元URLは，論文中およびドキュメントに記載する．
